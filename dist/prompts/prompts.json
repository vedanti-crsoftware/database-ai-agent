{
    "prompt": "Considering the schema provided: {{schema}}. \n\nAnswer the following question using SQL: Question: {{question}} . Write a PostgreSQL-compatible SQL query to retrieve the requested information without any additional characters or spaces. Important: Always explicitly prefix all tables with the 'dbo.' schema. Use PostgreSQL's syntax and functions (like CURRENT_DATE, INTERVAL, etc.) rather than functions from other SQL dialects. Remove any delimiter if present like \\n or newline character. Please ensure the query is formatted correctly and only includes the necessary components. End with semicolon"
}
